import 'package:flutter/material.dart';

class MealArticlePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Meal Articles'),
      ),
      body: Center(
        child: Text('This is the Meal Articles page!'),
      ),
    );
  }
}
